# Bank Pin Code In Python
def validate_pin(pin):
  if ( len(pin)==4 or len(pin) == 6) and pin.isdigit()
print(validate_pin(input("Enter the Bank Pin Code"))
print(validate_pin(input("Enter the Bank Pin Code"))
print(validate_pin(input("Enter the Bank Pin Code"))
print(validate_pin(input("Enter the Bank Pin Code"))
print(validate_pin(input("Enter the Bank Pin Code"))
